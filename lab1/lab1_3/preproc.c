
int factorial(int n) {
  if (n == 0)
    return 1;
  else
    return n * factorial(n - 1);
}

int plus(int x, int y) {
  return x + y;
}

#define PLUS(x, y) ((x) + (y))


int main(){
	int val;
	val =factorial(n);

	return 0;
}
